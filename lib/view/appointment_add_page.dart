import 'package:flutter/material.dart';

class AppointmentAddPage extends StatelessWidget {
  const AppointmentAddPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
